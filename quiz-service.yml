server:
  port: 8083

spring:
  application:
    name: quiz-service

  datasource:
    url: jdbc:mysql://localhost:3306/quizmsdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
